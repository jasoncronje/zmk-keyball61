CONFIG_SPI=y
CONFIG_INPUT=y
CONFIG_ZMK_MOUSE=y
CONFIG_ZMK_EXT_POWER=y

# --- PMW3610 Driver Configuration ---
CONFIG_PMW3610=y
# Default trackball sensitivity (CPI/DPI). Higher = faster.
CONFIG_PMW3610_CPI=400
CONFIG_PMW3610_CPI_DIVIDOR=1
CONFIG_PMW3610_ORIENTATION_180=y
# Scroll speed divisor. Higher = slower scrolling.
CONFIG_PMW3610_SCROLL_TICK=32
# Invert scroll direction.
CONFIG_PMW3610_INVERT_SCROLL_X=y
CONFIG_PMW3610_INVERT_SCROLL_Y=n
# Enable smart algorithm for smoother movement.
CONFIG_PMW3610_SMART_ALGORITHM=y
CONFIG_PMW3610_RUN_DOWNSHIFT_TIME_MS=3264
CONFIG_PMW3610_REST1_SAMPLE_TIME_MS=20
#CONFIG_PMW3610_REST1_DOWNSHIFT_TIME_MS=9600
#CONFIG_PMW3610_REST2_SAMPLE_TIME_MS=
#CONFIG_PMW3610_REST2_DOWNSHIFT_TIME_MS=
#CONFIG_PMW3610_REST3_SAMPLE_TIME_MS=
#CONFIG_PMW3610_REST3_DOWNSHIFT_TIME_MS=
#CONFIG_PMW3610_FORCE_AWAKE=n
# Polling rate. Lower polling rate (125Hz) saves battery.
CONFIG_PMW3610_POLLING_RATE_125_SW=y

# Keep BLE motion below the host link cadence without changing USB behavior.
CONFIG_ZMK_BLE_MOUSE_REPORT_INTERVAL_MS=16

# Keep CPR available on the Mac-facing peripheral link so macOS can apply the
# requested interval and 4-second supervision timeout. The left half disables
# CPR, which keeps the split link off the controller-wide CPR mutex.
CONFIG_BT_CTLR_CONN_PARAM_REQ=y

# Prefer the more robust 1M PHY. HID traffic does not benefit from 2M throughput,
# and the live link showed heavy packet loss when competing Bluetooth links ran.
CONFIG_BT_CTLR_PHY_2M=n

CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n
